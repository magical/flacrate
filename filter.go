package main

// FixBytes fixes the flac file given as a byte slice in place,
// overwriting the sample rate in every frame with sampleRate.
func FixBytes(flacBytes []byte, sampleRate int) error { return nil }
